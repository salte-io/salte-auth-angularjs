sudo: false
language: node_js
node_js:
- 6.3
cache:
  directories:
  - node_modules
branches:
  only:
    - master
before_script:
  - npm prune
script:
  # TODO: Add Tests
  # - npm run test
  - npm run build
after_success:
# TODO: Add coverage support
#   - npm run report-coverage
  - npm run semantic-release
notifications:
  email: change
